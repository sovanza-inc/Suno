# NEXT_TASK — initial seed (will be overwritten by the loop's first plan_next phase)

_Generated by hand at bootstrap. After the first loop iteration runs, `src/suno/orchestrator/task_planner.py` will rewrite this file based on observed state._

## Current state

- Iteration: **0** (no training runs yet)
- Last WER: n/a
- Best WER: n/a
- Repo: just restructured to enterprise layout, pushed to GitHub

## Recommended next task

**Run the first smoke iteration end-to-end.**

```
cd /Users/faseehali/Desktop/Suno
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
make smoke
```

`make smoke` runs one iteration with tiny step + sample counts to confirm:
- FLEURS ur_pk downloads (~250 MB)
- whisper-small downloads (~470 MB)
- Training step actually runs on MPS without OOM
- Eval emits a `FINAL_WER` line
- Adapter saves to `models/whisper-small-lora-iter0001/`
- `PROGRESS.md` gets a journal entry
- `NEXT_TASK.md` gets regenerated based on the real state

Expected runtime: ~5-15 minutes on M5 Pro for downloads + 20 train steps + 5 eval samples.

If green, install as a service so it keeps running while you sleep:

```
make loop-install      # launchd auto-start at login
tail -f logs/launchd.out.log
```

## After that

The loop will regenerate this file each iteration. Read it at the start of every session. If it ever looks stale, regenerate manually with `suno-plan`.

## Standing context

- See `CLAUDE.md` for session protocol
- See `docs/decisions/` for locked architectural choices
- See `PROGRESS.md` for full history
